import styled from "styled-components";

export const Top = () => {
  return (
    <SContainer>
      <h2>トップ</h2>
    </SContainer>
  );
};

const SContainer = styled.div`
  text-align: center;
`;
